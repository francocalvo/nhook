[project]
name = "notion-hook"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
nhook = "notion_hook:app"

[build-system]
requires = ["hatchling", "editables"]
build-backend = "hatchling.build"

